reference/*
# NOTES-FROM-PLANNING.md entry 110: the ballistic solver's references are committed; everything else under reference/ stays local.
!reference/ballistics-js/
!reference/ballistics-cases.json
!reference/py-ballisticcalc/
!reference/drag-tables/
bin/
obj/
*.user
TestResults/
out/
Claude outputs/
excluded/
scans/mounted/

# NOTES-FROM-PLANNING.md entry 128: the site is built into website/_site/, which is never committed.
website/_site/

# Entry 144: Python's own build output. website/server/__pycache__ was committed by accident and the .pyc went stale
# the moment the script beside it changed, so the repository carried a compiled copy of a script it no longer matched.
__pycache__/
*.pyc

# Entry 143 section 1.2: two research scripts build PDFs beside themselves. Nothing on the site links them,
# the runner has no reportlab to rebuild them, and a generated file in the repository goes stale silently.
website/research/*/figures/*.pdf
