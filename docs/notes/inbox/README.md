# Inbox for notes from the planning session

The planning session delivers each new entry here as its own file, `entry-NN.md`, and never modifies any existing file in this repository. The Claude Code session is the only writer of `docs/NOTES-FROM-PLANNING.md`: folding an entry into the top of that log, setting its status and deleting the file from this directory are part of actioning it.
