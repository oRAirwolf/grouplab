<?xml version="1.0" encoding="utf-8"?>
<!--
  NOTES-FROM-PLANNING.md entry 224 section 3: GroupLab as an MSIX package for the Microsoft Store, which signs it on submission, so no
  certificate is needed here. scripts/package-msix.ps1 fills the four values in braces: the identity name and publisher Partner Center gives
  when the name GroupLab is reserved, the publisher's display name, and the version. Windows 10 version 1809 is the Store's floor for MSIX.
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap rescap">

  <Identity Name="{IdentityName}" Publisher="{Publisher}" Version="{Version}" ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>GroupLab</DisplayName>
    <PublisherDisplayName>{PublisherDisplayName}</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
    <Description>Measures a group of shots on a printed target from a scan or a photograph.</Description>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="GroupLab" Executable="GroupLab.App.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements DisplayName="GroupLab" Description="Measures a group of shots on a printed target."
                          BackgroundColor="transparent" Square150x150Logo="Assets\Square150x150Logo.png" Square44x44Logo="Assets\Square44x44Logo.png">
        <uap:DefaultTile Wide310x150Logo="Assets\Wide310x150Logo.png" />
      </uap:VisualElements>
    </Application>
  </Applications>

  <Capabilities>
    <Capability Name="internetClient" />
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
